name: Windows Build v0.7

on:
  push:
    branches:
      - gameplay-path-dimensions-v06
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build and verify narrative arcs
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up MSYS2 MinGW
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          path-type: inherit
          install: >-
            mingw-w64-ucrt-x86_64-gcc

      - name: Add MinGW to PATH
        shell: pwsh
        run: echo "C:\msys64\ucrt64\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Build game
        shell: cmd
        run: |
          build.bat > build.log 2>&1
          set BUILD_EXIT=%ERRORLEVEL%
          type build.log
          exit /b %BUILD_EXIT%

      - name: Verify patched source
        shell: pwsh
        run: |
          $source = Get-Content src/wendao_enhanced.cpp -Raw -Encoding UTF8
          @(
            "V0_6_PATH_DIMENSIONS",
            "V0_7_NARRATIVE_ARCS",
            "SAVE_V5",
            "STORY_STATE_V2",
            "NarrativeArcState",
            "BuildNarrativeArcDigest",
            "【旧玉·一】夜半微温",
            "【旧玉·终】不借旧我",
            "【山门·终】自择道统",
            "【家世·终】认或不认",
            "【战帖·终】敌友由我"
          ) | ForEach-Object {
            if (-not $source.Contains($_)) { throw "Missing v0.7 marker: $_" }
          }
          if (-not (Test-Path release/wendao_enhanced.exe)) { throw "Executable missing" }

      - name: Verify patch idempotence
        shell: pwsh
        run: |
          python tools/apply_v06_path_dimensions.py
          python tools/repair_v06_path_digest.py
          python tools/apply_v07_story_arcs.py
          if ($LASTEXITCODE -ne 0) { throw "Patch idempotence check failed" }

      - name: Runtime Agent JSON smoke
        shell: pwsh
        run: |
          $release = (Resolve-Path release).Path
          $statePath = Join-Path $release "v07_agent_state.json"
          $commandPath = Join-Path $release "v07_agent_command.txt"
          $stdoutPath = Join-Path $release "v07_runtime_stdout.log"
          $stderrPath = Join-Path $release "v07_runtime_stderr.log"
          Remove-Item $statePath,$commandPath,$stdoutPath,$stderrPath -ErrorAction SilentlyContinue

          $env:WENDAO_AGENT_STATE = $statePath
          $env:WENDAO_AGENT_COMMAND = $commandPath
          $env:WENDAO_AGENT_HIDE = "1"
          $env:WENDAO_TRACE_HIDE = "1"

          $proc = Start-Process -FilePath (Join-Path $release "wendao_enhanced.exe") `
            -WorkingDirectory $release -PassThru `
            -RedirectStandardOutput $stdoutPath -RedirectStandardError $stderrPath
          try {
            $deadline = (Get-Date).AddSeconds(25)
            while ((Get-Date) -lt $deadline -and -not (Test-Path $statePath)) {
              Start-Sleep -Milliseconds 250
            }
            if (-not (Test-Path $statePath)) { throw "Agent state file was not created" }

            $state = $null
            while ((Get-Date) -lt $deadline) {
              try {
                $state = Get-Content $statePath -Raw -Encoding UTF8 | ConvertFrom-Json
                if ($state.player) { break }
              } catch {}
              Start-Sleep -Milliseconds 250
            }
            if (-not $state.player) { throw "Agent state JSON was not readable" }

            @("daoHeart", "reputation", "enmity", "effectiveKarma", "storyArcs") | ForEach-Object {
              if ($null -eq $state.player.PSObject.Properties[$_]) {
                throw "Runtime Agent JSON missing player.$_"
              }
            }
            if ($state.player.storyArcs -notmatch "旧玉0/4" -or
                $state.player.storyArcs -notmatch "山门0/4" -or
                $state.player.storyArcs -notmatch "家世0/4" -or
                $state.player.storyArcs -notmatch "战帖0/4") {
              throw "Unexpected initial story arc digest: $($state.player.storyArcs)"
            }
            if ($state.state -ne "MENU") { throw "Expected MENU state, got $($state.state)" }

            "00001 START v07-smoke" | Set-Content $commandPath -Encoding UTF8
            $startDeadline = (Get-Date).AddSeconds(12)
            do {
              Start-Sleep -Milliseconds 300
              try { $state = Get-Content $statePath -Raw -Encoding UTF8 | ConvertFrom-Json } catch {}
            } while ((Get-Date) -lt $startDeadline -and $state.state -eq "MENU")
            if ($state.state -eq "MENU") { throw "Agent START command was not consumed" }
            if ([string]::IsNullOrWhiteSpace($state.player.storyArcs)) {
              throw "Story arc digest disappeared after starting game"
            }

            $summary = [ordered]@{
              state = $state.state
              daoHeart = $state.player.daoHeart
              reputation = $state.player.reputation
              enmity = $state.player.enmity
              karma = $state.player.karma
              effectiveKarma = $state.player.effectiveKarma
              storyArcs = $state.player.storyArcs
            }
            $summary | ConvertTo-Json | Set-Content (Join-Path $release "v07_runtime_summary.json") -Encoding UTF8
            "00002 QUIT" | Set-Content $commandPath -Encoding UTF8
          }
          finally {
            Start-Sleep -Milliseconds 500
            if (-not $proc.HasExited) { Stop-Process -Id $proc.Id -Force }
          }

      - name: Upload build output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wendao-v07-windows-build
          path: |
            build.log
            release/
          if-no-files-found: warn